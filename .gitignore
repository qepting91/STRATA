data/*
!data/.gitkeep
.env
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# docs/ holds locally-provided reference PDFs (e.g. the SANS ICS Cyber
# Kill Chain paper and the Dragos 2026 OT/ICS Cybersecurity Year in
# Review report) used for analysis this project never re-serves or
# reposts (see .streamlit/config.toml's enableStaticServing=false and
# the SANS paper's own "no reposting" notice) -- never committed.
docs/
reports/
.claude/
